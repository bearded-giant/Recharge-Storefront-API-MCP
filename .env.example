# Recharge Storefront API Configuration

# Store Configuration
# Your Shopify store domain (required - must end with .myshopify.com)
RECHARGE_STOREFRONT_DOMAIN=your-shop.myshopify.com

# Authentication
# Recharge Customer Session Token (conditional - can be provided per tool call)
# This must be extracted from a customer's authenticated Recharge portal session
# Token is customer-scoped and temporary (expires with customer session)
# The MCP server cannot create these tokens - they must come from portal authentication
RECHARGE_SESSION_TOKEN=session_token_from_customer_portal_auth


# MCP Server Configuration
# Server identification (optional - for logging and debugging)
MCP_SERVER_NAME=recharge-storefront-api-mcp
MCP_SERVER_VERSION=1.0.0

# Development Configuration