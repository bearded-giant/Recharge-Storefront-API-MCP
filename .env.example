# Recharge Storefront API Configuration

# Store Configuration
# Your Shopify store domain (required - must end with .myshopify.com)
RECHARGE_STOREFRONT_DOMAIN=your-shop.myshopify.com

# Authentication
# Recharge Customer Session Token (conditional - can be provided per tool call)
# This is generated when a customer logs into the Recharge portal
# Token is customer-scoped and temporary (expires with session)
RECHARGE_SESSION_TOKEN=your_customer_session_token_here


# MCP Server Configuration
# Server identification (optional - for logging and debugging)
MCP_SERVER_NAME=recharge-storefront-api-mcp
MCP_SERVER_VERSION=1.0.0

# Development Configuration